<%= form_for @classroom do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.label :shift %><br>
    <%= f.text_field :shift %><br>
    <%= f.label :professor %><br>

    <%= f.collection_select :professor_ids, @professors, :id, :id, {},
    {:multiple => true}%><br>

    <%= f.label :student %><br>

    <%= f.collection_select :student_ids, @students, :id, :id, {},
    {:multiple => true}%><br>

  <p>
    <%= f.submit "Save" %>
  </p>
<% end %>
